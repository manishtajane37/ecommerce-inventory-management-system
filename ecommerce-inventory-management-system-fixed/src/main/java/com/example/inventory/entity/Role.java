package com.example.inventory.entity;

/**
 * Defines the roles available in the system.
 * ADMIN can manage products/categories; USER can only view them.
 */
public enum Role {
    ADMIN,
    USER
}
